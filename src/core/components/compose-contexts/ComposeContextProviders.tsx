

/**
 * Accepts a list of providers to help avoid huge nesting in App.tsx
 * <ComposeContextProviders providers = [] />
 *
 * @return {*} 
 */
function ComposeContextProviders() {
  return <div>ComposeContextProviders</div>;
}

export default ComposeContextProviders;
